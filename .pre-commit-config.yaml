# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: rustfmt
        name: rustfmt (changed files)
        entry: bash -lc 'rustfmt --edition 2021 "$@"'
        language: system
        types: [rust]

